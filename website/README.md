# CamHealth Website

Informational Next.js 14 site — what CamHealth is, its features, and product updates.
Deployed to Vercel (see `.github/workflows/website-deploy.yml`).

```bash
npm install
npm run dev     # http://localhost:3000
npm run build
```
