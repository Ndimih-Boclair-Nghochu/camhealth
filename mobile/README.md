# CamHealth Mobile

Companion app (Expo / React Native) for clinicians and patients — Phase 2.
Consumes the same API (`../server`) and shared types (`../shared`) as the desktop app.

```bash
npm install
npm start        # open in Expo Go, or run on Android/iOS
```
