Django>=5.2
djangorestframework>=3.16
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
python-dotenv>=1.0
qrcode[pil]>=7.4
# For PostgreSQL in production (on-site server / cloud). SQLite is used by default in dev.
# psycopg[binary]>=3.2
gunicorn>=22.0
